<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uw.sunny121.langxpert">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.VocabListApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LangXpert">
        <activity android:name=".quiz.QuizResultViewActivity"></activity>
        <activity android:name=".quiz.QuizActivity" />
        <activity android:name=".startingScreenActivity" />
        <activity android:name=".VocabDetailViewActivity" />
        <activity android:name=".AddVocabActivity" />
        <activity android:name=".VoacbCards" />
        <activity android:name=".VocabsActivity" />
        <activity android:name=".item_vocabLists" />
        <activity android:name=".VocabListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>